# dice-rolller
Dice Roller is a simple command-line application built with Python that simulates rolling a six-sided die. Each time the user rolls the dice, the program generates a random number between 1 and 6. Users can continue rolling as many times as they like or exit the application. 
